import { PageHeader, TopicList } from '@/components/ui'; import { adminUsers } from '@/lib/mock-data';
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const u=adminUsers.find(x=>x.user.id===id)??adminUsers[0];return <main className="p-8"><PageHeader kicker="Internal review" title={`${u.user.preferredName}: review summary`}><p>Admin prototype avoids raw transcripts by default and shows derived status, coverage, contradictions, risks, and readiness.</p></PageHeader><div className="card mb-6 p-5"><p><b>Verification:</b> {u.user.verificationStatus}</p><p><b>Trust:</b> {u.user.trustStatus}</p><p><b>Risk flags:</b> {u.riskFlags.join(', ')}</p></div><TopicList/></main>}

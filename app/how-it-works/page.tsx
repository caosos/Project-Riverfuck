import { PublicNav, PageHeader } from '@/components/ui';
export default function Page(){return <><PublicNav/><main className="mx-auto max-w-5xl p-6"><PageHeader kicker="Process" title="Conversation builds the model."><p>Users do not market themselves to strangers. They talk privately with an AI interviewer that asks follow-up questions, detects contradictions, and turns evidence into a correctable model.</p></PageHeader><div className="grid gap-4 md:grid-cols-3">{['Private interview','Evidence model','Readiness before recommendation'].map((x,i)=><div className="card p-6" key={x}><p className="label">Step {i+1}</p><h2 className="mt-3 text-2xl font-black">{x}</h2><p className="mt-3 text-stone-600">{i===0?'The AI learns values, conflict, family, lifestyle, trust, boundaries, and attraction as one category.':i===1?'Users review summaries, evidence snippets, confidence, importance, and match logic.':'Hard gates and low confidence can stop a weak recommendation.'}</p></div>)}</div></main></>}

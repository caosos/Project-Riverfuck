import { PageHeader } from '@/components/ui';
const fields=['Preferred name','DOB or age range placeholder','Location city/state','Relationship status declaration','Relationship intent','Children status / preference','Location radius','Willingness to relocate','Truthfulness acknowledgment','Privacy acknowledgment'];
export default function Page(){return <><PageHeader kicker="Initial setup" title="Onboarding collects only what is needed to begin."><p>This is not a 500-question form. The interview expands the model through conversation.</p></PageHeader><div className="card grid gap-4 p-6 md:grid-cols-2">{fields.map(f=><label key={f} className="grid gap-2 text-sm font-bold">{f}<input className="rounded-xl border border-stone-200 bg-white p-3 font-normal" placeholder="Prototype field"/></label>)}</div></>}

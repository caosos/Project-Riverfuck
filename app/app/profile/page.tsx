import { PageHeader } from '@/components/ui'; import { seedProfile } from '@/lib/mock-data';
export default function Page(){return <><PageHeader kicker="Private model" title="Evidence-based compatibility profile"><p>Every category is correctable. Raw private conversations are not shown to matches.</p></PageHeader><div className="grid gap-4">{seedProfile.categories.map(c=><div className="card p-5" key={c.id}><div className="flex flex-wrap justify-between gap-3"><h2 className="text-2xl font-black">{c.categoryName}</h2><span className="pill">{c.confidenceLevel} · {c.importanceLevel}</span></div><p className="mt-3 text-stone-700">{c.summary}</p><p className="mt-3 text-sm"><b>Match logic:</b> {c.matchLogic.replaceAll('_',' ')}</p><div className="mt-3 grid gap-2">{c.observations.map(o=><div className="rounded-xl bg-stone-100 p-3" key={o.trait}><b>{o.trait}</b><p className="text-sm text-stone-600">Evidence: {o.evidence}</p></div>)}</div><div className="mt-4 flex flex-wrap gap-2">{['Accurate','Partly accurate','Inaccurate','Add clarification','Ask me more about this'].map(x=><button className="pill" key={x}>{x}</button>)}</div></div>)}</div></>}
